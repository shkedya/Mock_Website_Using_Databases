<!doctype html>
<html lang="en-US">
<head>
<meta charset="utf-8">
<link rel= "stylesheet" type= "text/css" href= "{{ url_for('static',filename='styles/mart_info.css') }}">
</head>

<body>
<!-- Main Container -->
<div class="container"> 
  <!-- Navigation -->
  <header> <a href="">
    <h4 class="logo">POKÉ MART&nbsp;</h4>
    </a>
    <nav>
      <ul>
        <li><a href="index.html">HOME</a></li>
        <li><a href="customers.html">CUSTOMERS</a></li>
		<li><a href="items.html">ITEMS&nbsp;</a></li>
		<li> <a href="orders.html">ORDERS&nbsp;</a></li>
        <li> <a href="trades.html">TRADES</a></li>
      </ul>
    </nav>
  </header>
  <section class="myHeader" id="myHeader">
	  <h2 class="myHeader_header">Customers</h2>
  </section>
  <table class=database_table>
    <thead>
      <th>ID</th>
      <th>Name</th>
      <th>Phone Number</th>
      <th >Badges</th>
      <th> Gender</th>
    </thead>
    <tbody>
      {% for val in customer_data %}
      <tr>
        <td>#{{val['Customer_id']}}
        <td>{{val['Name']}}
        <td>{{val['Phone_number']}}
        <td>{{val['Badges']}}
        <td>{{val['Gender']}}
        <td><form method="get" action="/delete-customer/{{val['Customer_id']}}">
    <button type="submit">Delete</button></form>
        <td><form action="customers_update.html">
      <input type="submit" value="Update"/></form>
      </tr>
      {% endfor %}
    </tbody>
  </table>

    <br />
    <h2>Insert</h2>
    <form method = "post">
        <div class="form-group">
            <label>Name<input type="text" name="Name" value="" /></label>
            <label>Phone_number<input type="text" name="Phone_number" value="" /></label>
            <label>Badges<input type="number" name="Badges" value="" /></label>
            <label>Gender<input type="text" name="Gender" value="" /></label>
            <input type="submit" name="insert_customer_submit" id="new_Row" value="Insert Customer" class="button"/>
        </div>
        <form id="insertCustomer">
    </form>
    <br />

  <script>
    function filterTable(){
      // Declare variables
      var input, filter, table, tr, td, i, txtValue;
      input = document.getElementById("myInput");
      filter = input.value.toUpperCase();
      table = document.getElementById("myTable");
      tr = table.getElementsByTagName("tr");
    }
    </script>

  </div>
<!-- Main Container Ends -->
</body>
</html>
